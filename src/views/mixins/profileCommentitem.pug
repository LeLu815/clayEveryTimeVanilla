mixin profileCommentitem(item)
  div
    a(href=`/content/playgroundComment/${item.contentOwnName}`)  
      div 여기에 이미지
      div
        div 
          div 유저 사진 여기
          div #{item.owner.displayName}
        div 
          div #{item.commentText}
        div
          div #{item.createdAt}
          div #{item.comment}
          div #{item.likes}
    div 
      div 삭제
      input(type="checkbox", name="isDel", value=`${item.newCommentOwnName}`)
      input(type="hidden", name="contentIds", value=`${item.contentOwnName}`)

      